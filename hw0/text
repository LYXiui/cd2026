在C語言中，最有效率且符合限制的方法是使用直接定址表(Direct Addressing Table）概念實作雜湊表（Hash Table）。
1.在標準的Linked List中，如果要確認新讀取的字元是否已經存在，程式必須從head開始，沿著指標逐一比對每個節點的character。最好情況是字元就在第一個節點，此時複雜度為O(1)。最壞情況是字元在最後一個節點或不存在，必須走訪所有n個節點，因此時間複雜度為O(n)。
2.降低複雜度的方法可以選擇建立一個指標陣列作為索引。這樣一來，尋找字元是否存在就不需要走訪串列，只需透過陣列索引直接存取。這時的時間複雜度和空間複雜度都是O(1)。main1.c即為此實作。
